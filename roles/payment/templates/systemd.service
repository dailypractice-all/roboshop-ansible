[Unit]
Description=Payment Service

[Service]
User=root
WorkingDirectory=/home/roboshop/payment

Environment=CART_HOST=cart.dailypractice.internal
Environment=USER_HOST=user.dailypractice.internal
Environment=AMQP_HOST=rabbitmq.dailypractice.internal
Environment=AMQP_USER=roboshop
Environment=AMQP_PASS={{RABBITMQ_USER_PASSWORD}}

ExecStart=/usr/local/bin/uwsgi --ini payment.ini
ExecStop=/bin/kill -9 $MAINPID
SyslogIdentifier=payment

[Install]
WantedBy=multi-user.target